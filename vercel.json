{
  "version": 2,
  "builds": [
    { "src": "frontend/**", "use": "@vercel/static" }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/(.*)", "dest": "https://YOUR_BACKEND_DOMAIN/api/$1" },
    { "src": "/(.*)", "dest": "/frontend/$1" }
  ]
}